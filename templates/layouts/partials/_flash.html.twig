{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label == 'error' ? 'danger' : (label == 'info' ? 'info' : (label == 'success' ? 'success' : 'secondary')) }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}